# 🏗️ Архитектура FOMO Auction

## Обзор системы

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend                              │
│                    (React + TailwindCSS)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   Home.js   │  │  Auction.js │  │  Holdings   │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│         │                │                │                  │
│         └────────────────┼────────────────┘                  │
│                          │                                   │
│              ┌───────────┴───────────┐                      │
│              │     Components        │                      │
│              │   (auction/, home/)   │                      │
│              └───────────────────────┘                      │
└─────────────────────────┬───────────────────────────────────┘
                          │ HTTP/REST
                          │ /api/*
┌─────────────────────────┴───────────────────────────────────┐
│                        Backend                               │
│                       (FastAPI)                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                    server.py                         │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │   │
│  │  │ /api/    │  │/treasury │  │/auction- │          │   │
│  │  │          │  │          │  │  stats   │          │   │
│  │  └──────────┘  └──────────┘  └──────────┘          │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────┴───────────────────────────────────┐
│                       MongoDB                                │
│                    (motor async)                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │  users   │  │   bids   │  │   nfts   │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

## Frontend архитектура

### Структура директорий

```
frontend/src/
├── components/
│   ├── auction/           # 21 компонент для страницы Auction
│   │   ├── index.js       # Barrel export
│   │   ├── AuctionHeroSection.js
│   │   ├── AuctionCTASection.js
│   │   ├── AuctionChart.js
│   │   ├── PlaceBidPanel.js
│   │   ├── BidModal.js
│   │   ├── LastChanceSection.js
│   │   ├── NFTBoxCollectionSection.js
│   │   ├── NFTUtilitySection.js
│   │   ├── UserEvolutionSection.js
│   │   ├── FOMOUniverseSection.js
│   │   └── ... (и другие)
│   │
│   ├── home/              # Компоненты главной страницы
│   │   ├── HeroSection.js
│   │   ├── CTASection.js
│   │   └── ...
│   │
│   ├── ui/                # Shadcn UI компоненты
│   │   ├── button.jsx
│   │   ├── card.jsx
│   │   └── ...
│   │
│   ├── Header.js          # Навигация
│   ├── Footer.js          # Подвал
│   └── CookieConsent.js   # Cookie баннер
│
├── pages/
│   ├── Auction.js         # Страница аукциона (202 строки)
│   └── Home.js            # Главная страница
│
├── utils/
│   └── nftUtils.js        # Утилиты для NFT (getRarity)
│
├── App.js                 # Корневой компонент + роутинг
└── index.js               # Entry point
```

### Поток данных

```
App.js
  │
  ├── Header (навигация)
  │
  ├── currentPage === 'home'
  │   └── Home.js
  │       ├── HeroSection
  │       ├── Features
  │       ├── NFTUtility
  │       └── CTASection
  │
  └── currentPage === 'auction'
      └── Auction.js
          ├── State (timeLeft, showBidModal, etc.)
          ├── AuctionHeroSection
          ├── Main Grid
          │   ├── AuctionChart
          │   ├── RecentActivity
          │   ├── HowAuctionWorks
          │   ├── RaritySection
          │   ├── PlaceBidPanel
          │   ├── GamificationMechanics
          │   └── ...
          ├── LastChanceSection
          ├── NFTBoxCollectionSection
          ├── NFTUtilitySection
          ├── UserEvolutionSection
          ├── FOMOUniverseSection
          └── AuctionCTASection
```

## Backend архитектура

### server.py структура

```python
# Конфигурация
app = FastAPI()
MONGO_URL = os.environ.get('MONGO_URL')

# Middleware
app.add_middleware(CORSMiddleware, ...)

# Routes
@app.get("/api/")           # Health check
@app.get("/api/treasury")    # Treasury данные
@app.get("/api/auction-stats") # Статистика аукциона
```

### Текущее состояние API

⚠️ **Важно**: На данный момент API возвращает **статические mock данные**. Интеграция с MongoDB и реальная бизнес-логика запланированы.

## Стилизация

### TailwindCSS

Используется TailwindCSS с кастомной конфигурацией:

```javascript
// tailwind.config.js
module.exports = {
  content: ['./src/**/*.{js,jsx}'],
  theme: {
    extend: {
      colors: {
        emerald: { ... },
        // Кастомные цвета
      }
    }
  }
}
```

### CSS классы проекта

```css
/* App.css */
.card { ... }              /* Карточки */
.animate-pulse-scale { ... } /* Пульсирующая анимация */
.scrollbar-hide { ... }    /* Скрытие скроллбара */
```

## Паттерны

### Компонентный подход

Все UI разбит на переиспользуемые компоненты:

```jsx
// Плохо ❌
<div className="...много классов...">
  // Много JSX
</div>

// Хорошо ✅
<NFTBoxCollectionSection />
<UserEvolutionSection />
```

### Barrel exports

```javascript
// components/auction/index.js
export { default as AuctionChart } from './AuctionChart';
export { default as PlaceBidPanel } from './PlaceBidPanel';
// ... все компоненты

// Использование
import { AuctionChart, PlaceBidPanel } from '../components/auction';
```

### Props drilling vs Context

Текущая реализация использует props drilling для простоты. При масштабировании рекомендуется:
- React Context для глобального состояния
- Redux/Zustand для сложной логики

## Масштабирование

### Планируемые улучшения

1. **State Management**
   - Добавить Redux или Zustand
   - Централизованное хранение состояния аукциона

2. **API Layer**
   - Добавить React Query для кэширования
   - Оптимистичные обновления

3. **Real-time**
   - WebSocket для live обновлений ставок
   - Server-Sent Events как альтернатива

4. **Authentication**
   - JWT токены
   - Web3 подпись сообщений

5. **Database**
   - Полная интеграция с MongoDB
   - Индексы для оптимизации запросов
